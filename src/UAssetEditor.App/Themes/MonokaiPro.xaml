<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- Monokai Pro "Classic" palette -->
    <Color x:Key="Mono.Background.Color">#FF2D2A2E</Color>
    <Color x:Key="Mono.Panel.Color">#FF221F22</Color>
    <Color x:Key="Mono.Surface.Color">#FF363537</Color>
    <Color x:Key="Mono.SurfaceRaised.Color">#FF403E41</Color>
    <Color x:Key="Mono.Border.Color">#FF5B595C</Color>
    <Color x:Key="Mono.Foreground.Color">#FFFCFCFA</Color>
    <Color x:Key="Mono.Muted.Color">#FF939293</Color>
    <Color x:Key="Mono.Red.Color">#FFFF6188</Color>
    <Color x:Key="Mono.Orange.Color">#FFFC9867</Color>
    <Color x:Key="Mono.Yellow.Color">#FFFFD866</Color>
    <Color x:Key="Mono.Green.Color">#FFA9DC76</Color>
    <Color x:Key="Mono.Cyan.Color">#FF78DCE8</Color>
    <Color x:Key="Mono.Purple.Color">#FFAB9DF2</Color>

    <SolidColorBrush x:Key="Mono.Background" Color="{StaticResource Mono.Background.Color}" />
    <SolidColorBrush x:Key="Mono.Panel" Color="{StaticResource Mono.Panel.Color}" />
    <SolidColorBrush x:Key="Mono.Surface" Color="{StaticResource Mono.Surface.Color}" />
    <SolidColorBrush x:Key="Mono.SurfaceRaised" Color="{StaticResource Mono.SurfaceRaised.Color}" />
    <SolidColorBrush x:Key="Mono.Border" Color="{StaticResource Mono.Border.Color}" />
    <SolidColorBrush x:Key="Mono.Foreground" Color="{StaticResource Mono.Foreground.Color}" />
    <SolidColorBrush x:Key="Mono.Muted" Color="{StaticResource Mono.Muted.Color}" />
    <SolidColorBrush x:Key="Mono.Red" Color="{StaticResource Mono.Red.Color}" />
    <SolidColorBrush x:Key="Mono.Orange" Color="{StaticResource Mono.Orange.Color}" />
    <SolidColorBrush x:Key="Mono.Yellow" Color="{StaticResource Mono.Yellow.Color}" />
    <SolidColorBrush x:Key="Mono.Green" Color="{StaticResource Mono.Green.Color}" />
    <SolidColorBrush x:Key="Mono.Cyan" Color="{StaticResource Mono.Cyan.Color}" />
    <SolidColorBrush x:Key="Mono.Purple" Color="{StaticResource Mono.Purple.Color}" />

    <!-- Dirty-row highlight: yellow at low opacity so grid text over it stays legible -->
    <SolidColorBrush x:Key="Mono.DirtyHighlight" Color="{StaticResource Mono.Yellow.Color}" Opacity="0.28" />
    <SolidColorBrush x:Key="Mono.SelectionHighlight" Color="{StaticResource Mono.Purple.Color}" Opacity="0.35" />

    <!-- ===================== Window / base text ===================== -->

    <Style TargetType="Window">
        <Setter Property="Background" Value="{StaticResource Mono.Background}" />
        <Setter Property="Foreground" Value="{StaticResource Mono.Foreground}" />
    </Style>

    <Style TargetType="TextBlock">
        <Setter Property="Foreground" Value="{StaticResource Mono.Foreground}" />
    </Style>

    <Style TargetType="Label">
        <Setter Property="Foreground" Value="{StaticResource Mono.Foreground}" />
    </Style>

    <!-- ===================== ScrollBar (flat, minimal) ===================== -->

    <Style x:Key="Mono.ScrollBarLineButton" TargetType="RepeatButton">
        <Setter Property="Focusable" Value="False" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="RepeatButton">
                    <Border Background="Transparent" />
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="Mono.ScrollBarPageButton" TargetType="RepeatButton">
        <Setter Property="Focusable" Value="False" />
        <Setter Property="IsTabStop" Value="False" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="RepeatButton">
                    <Border Background="Transparent" />
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="Mono.ScrollBarThumb" TargetType="Thumb">
        <Setter Property="Focusable" Value="False" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Thumb">
                    <Border Background="{StaticResource Mono.Border}" CornerRadius="3" Margin="2" />
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="ScrollBar">
        <Setter Property="Background" Value="{StaticResource Mono.Panel}" />
        <Setter Property="Width" Value="14" />
        <Setter Property="MinWidth" Value="14" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ScrollBar">
                    <Grid Background="{TemplateBinding Background}">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="0" />
                            <RowDefinition Height="*" />
                            <RowDefinition Height="0" />
                        </Grid.RowDefinitions>
                        <Track Grid.Row="1" Name="PART_Track" IsDirectionReversed="True">
                            <Track.DecreaseRepeatButton>
                                <RepeatButton Style="{StaticResource Mono.ScrollBarPageButton}" Command="ScrollBar.PageUpCommand" />
                            </Track.DecreaseRepeatButton>
                            <Track.Thumb>
                                <Thumb Style="{StaticResource Mono.ScrollBarThumb}" />
                            </Track.Thumb>
                            <Track.IncreaseRepeatButton>
                                <RepeatButton Style="{StaticResource Mono.ScrollBarPageButton}" Command="ScrollBar.PageDownCommand" />
                            </Track.IncreaseRepeatButton>
                        </Track>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="Orientation" Value="Horizontal">
                <Setter Property="Height" Value="14" />
                <Setter Property="MinHeight" Value="14" />
                <Setter Property="Width" Value="Auto" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="ScrollBar">
                            <Grid Background="{TemplateBinding Background}">
                                <Track Name="PART_Track">
                                    <Track.DecreaseRepeatButton>
                                        <RepeatButton Style="{StaticResource Mono.ScrollBarPageButton}" Command="ScrollBar.PageLeftCommand" />
                                    </Track.DecreaseRepeatButton>
                                    <Track.Thumb>
                                        <Thumb Style="{StaticResource Mono.ScrollBarThumb}" />
                                    </Track.Thumb>
                                    <Track.IncreaseRepeatButton>
                                        <RepeatButton Style="{StaticResource Mono.ScrollBarPageButton}" Command="ScrollBar.PageRightCommand" />
                                    </Track.IncreaseRepeatButton>
                                </Track>
                            </Grid>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- ===================== Button ===================== -->

    <Style x:Key="Mono.ButtonBase" TargetType="Button">
        <Setter Property="Background" Value="{StaticResource Mono.SurfaceRaised}" />
        <Setter Property="Foreground" Value="{StaticResource Mono.Foreground}" />
        <Setter Property="BorderBrush" Value="{StaticResource Mono.Border}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Padding" Value="6,2" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="Chrome" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="3">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"
                                           Margin="{TemplateBinding Padding}" RecognizesAccessKey="True" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Chrome" Property="BorderBrush" Value="{StaticResource Mono.Purple}" />
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="Chrome" Property="Background" Value="{StaticResource Mono.Panel}" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Foreground" Value="{StaticResource Mono.Muted}" />
                            <Setter TargetName="Chrome" Property="Background" Value="{StaticResource Mono.Panel}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <Style TargetType="Button" BasedOn="{StaticResource Mono.ButtonBase}" />

    <!-- Reserved for actions with real consequences (Apply, Cancel, Repack) -->
    <Style x:Key="Mono.AccentButton" TargetType="Button" BasedOn="{StaticResource Mono.ButtonBase}">
        <Setter Property="Background" Value="{StaticResource Mono.Red}" />
        <Setter Property="Foreground" Value="{StaticResource Mono.Background}" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="BorderBrush" Value="{StaticResource Mono.Red}" />
    </Style>

    <!-- Hand-drawn title-bar caption buttons (see MainWindow.xaml) - flat, no border, wide hover target. -->
    <Style x:Key="Mono.TitleBarButton" TargetType="Button" BasedOn="{StaticResource Mono.ButtonBase}">
        <Setter Property="Width" Value="42" />
        <Setter Property="Height" Value="34" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Foreground" Value="{StaticResource Mono.Muted}" />
        <Setter Property="FontFamily" Value="Segoe UI" />
        <Setter Property="FontSize" Value="13" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="Chrome" Background="{TemplateBinding Background}">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Chrome" Property="Background" Value="{StaticResource Mono.SurfaceRaised}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ===================== TextBox ===================== -->

    <Style TargetType="TextBox">
        <Setter Property="Background" Value="{StaticResource Mono.Surface}" />
        <Setter Property="Foreground" Value="{StaticResource Mono.Foreground}" />
        <Setter Property="BorderBrush" Value="{StaticResource Mono.Border}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="CaretBrush" Value="{StaticResource Mono.Foreground}" />
        <Setter Property="SelectionBrush" Value="{StaticResource Mono.Purple}" />
        <Setter Property="Padding" Value="3" />
        <Style.Triggers>
            <Trigger Property="IsFocused" Value="True">
                <Setter Property="BorderBrush" Value="{StaticResource Mono.Purple}" />
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Foreground" Value="{StaticResource Mono.Muted}" />
                <Setter Property="Background" Value="{StaticResource Mono.Panel}" />
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- ===================== CheckBox ===================== -->

    <Style TargetType="CheckBox">
        <Setter Property="Foreground" Value="{StaticResource Mono.Foreground}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="CheckBox">
                    <StackPanel Orientation="Horizontal">
                        <Border x:Name="Box" Width="15" Height="15" Background="{StaticResource Mono.Surface}"
                                BorderBrush="{StaticResource Mono.Border}" BorderThickness="1" CornerRadius="2" VerticalAlignment="Center">
                            <Path x:Name="Check" Data="M 1,4 L 4,7 L 9,1" Stroke="{StaticResource Mono.Green}" StrokeThickness="2"
                                  StrokeStartLineCap="Round" StrokeEndLineCap="Round" StrokeLineJoin="Round"
                                  HorizontalAlignment="Center" VerticalAlignment="Center" Visibility="Collapsed" />
                        </Border>
                        <ContentPresenter Margin="6,0,0,0" VerticalAlignment="Center" RecognizesAccessKey="True" />
                    </StackPanel>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="Check" Property="Visibility" Value="Visible" />
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Box" Property="BorderBrush" Value="{StaticResource Mono.Purple}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ===================== ComboBox ===================== -->

    <!-- Spans the ComboBox's full width so a click anywhere on the control opens the
         dropdown (not just on the arrow glyph) - the arrow itself is pinned to a narrow
         right-hand column within this same hit-test area, purely for visual placement. -->
    <Style x:Key="Mono.ComboBoxToggleButton" TargetType="ToggleButton">
        <Setter Property="Focusable" Value="False" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ToggleButton">
                    <Grid Background="Transparent">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="22" />
                        </Grid.ColumnDefinitions>
                        <Path Grid.Column="1" Data="M 0,0 L 4,4 L 8,0" Stroke="{StaticResource Mono.Muted}" StrokeThickness="1.5"
                              HorizontalAlignment="Center" VerticalAlignment="Center" />
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="ComboBoxItem">
        <Setter Property="Background" Value="{StaticResource Mono.Surface}" />
        <Setter Property="Foreground" Value="{StaticResource Mono.Foreground}" />
        <Setter Property="Padding" Value="6,3" />
        <Style.Triggers>
            <Trigger Property="IsHighlighted" Value="True">
                <Setter Property="Background" Value="{StaticResource Mono.SelectionHighlight}" />
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style TargetType="ComboBox">
        <Setter Property="Background" Value="{StaticResource Mono.Surface}" />
        <Setter Property="Foreground" Value="{StaticResource Mono.Foreground}" />
        <Setter Property="BorderBrush" Value="{StaticResource Mono.Border}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Padding" Value="4,2" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ComboBox">
                    <Grid>
                        <Border Background="{StaticResource Mono.Surface}"
                                BorderBrush="{StaticResource Mono.Border}" BorderThickness="1" CornerRadius="3" />
                        <ToggleButton Style="{StaticResource Mono.ComboBoxToggleButton}"
                                      IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}" />
                        <ContentPresenter x:Name="ContentSite" IsHitTestVisible="False" HorizontalAlignment="Left" VerticalAlignment="Center"
                                          Margin="{TemplateBinding Padding}" Content="{TemplateBinding SelectionBoxItem}"
                                          ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}" />
                        <TextBox x:Name="PART_EditableTextBox" Visibility="Collapsed" Background="Transparent" Foreground="{StaticResource Mono.Foreground}" />
                        <Popup x:Name="Popup" Placement="Bottom" IsOpen="{TemplateBinding IsDropDownOpen}"
                               AllowsTransparency="True" Focusable="False" PopupAnimation="Slide">
                            <Border MinWidth="{TemplateBinding ActualWidth}" MaxHeight="{TemplateBinding MaxDropDownHeight}"
                                    Background="{StaticResource Mono.Surface}" BorderBrush="{StaticResource Mono.Border}" BorderThickness="1">
                                <ScrollViewer SnapsToDevicePixels="True">
                                    <ItemsPresenter KeyboardNavigation.DirectionalNavigation="Contained" />
                                </ScrollViewer>
                            </Border>
                        </Popup>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsFocused" Value="True">
                            <Setter Property="BorderBrush" Value="{StaticResource Mono.Purple}" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Foreground" Value="{StaticResource Mono.Muted}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ===================== GroupBox ===================== -->

    <Style TargetType="GroupBox">
        <Setter Property="Foreground" Value="{StaticResource Mono.Foreground}" />
        <Setter Property="BorderBrush" Value="{StaticResource Mono.Border}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="GroupBox">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>
                        <Border Grid.Row="0" Background="{StaticResource Mono.SurfaceRaised}"
                                BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="1,1,1,0" CornerRadius="4,4,0,0" Padding="8,4">
                            <ContentPresenter ContentSource="Header" TextElement.Foreground="{StaticResource Mono.Muted}" TextElement.FontWeight="SemiBold" />
                        </Border>
                        <Border Grid.Row="1" Background="{StaticResource Mono.Panel}"
                                BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="1" CornerRadius="0,0,4,4" Padding="6">
                            <ContentPresenter />
                        </Border>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ===================== ListBox ===================== -->

    <Style TargetType="ListBox">
        <Setter Property="Background" Value="{StaticResource Mono.Surface}" />
        <Setter Property="Foreground" Value="{StaticResource Mono.Foreground}" />
        <Setter Property="BorderBrush" Value="{StaticResource Mono.Border}" />
        <Setter Property="BorderThickness" Value="1" />
    </Style>

    <Style TargetType="ListBoxItem">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Foreground" Value="{StaticResource Mono.Foreground}" />
        <Setter Property="Padding" Value="4,2" />
        <Style.Triggers>
            <Trigger Property="IsSelected" Value="True">
                <Setter Property="Background" Value="{StaticResource Mono.SelectionHighlight}" />
            </Trigger>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{StaticResource Mono.SurfaceRaised}" />
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- ===================== TreeView ===================== -->

    <Style TargetType="TreeView">
        <Setter Property="Background" Value="{StaticResource Mono.Surface}" />
        <Setter Property="Foreground" Value="{StaticResource Mono.Foreground}" />
        <Setter Property="BorderBrush" Value="{StaticResource Mono.Border}" />
        <Setter Property="BorderThickness" Value="1" />
    </Style>

    <Style TargetType="TreeViewItem">
        <Setter Property="Foreground" Value="{StaticResource Mono.Foreground}" />
        <Setter Property="Padding" Value="2" />
        <Style.Triggers>
            <Trigger Property="IsSelected" Value="True">
                <Setter Property="Background" Value="{StaticResource Mono.SelectionHighlight}" />
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- ===================== ContextMenu / MenuItem ===================== -->

    <Style TargetType="ContextMenu">
        <Setter Property="Background" Value="{StaticResource Mono.Surface}" />
        <Setter Property="Foreground" Value="{StaticResource Mono.Foreground}" />
        <Setter Property="BorderBrush" Value="{StaticResource Mono.Border}" />
        <Setter Property="BorderThickness" Value="1" />
    </Style>

    <Style TargetType="MenuItem">
        <Setter Property="Background" Value="{StaticResource Mono.Surface}" />
        <Setter Property="Foreground" Value="{StaticResource Mono.Foreground}" />
        <Setter Property="Padding" Value="8,4" />
        <Style.Triggers>
            <Trigger Property="IsHighlighted" Value="True">
                <Setter Property="Background" Value="{StaticResource Mono.SelectionHighlight}" />
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- ===================== DataGrid ===================== -->

    <Style TargetType="DataGrid">
        <Setter Property="Background" Value="{StaticResource Mono.Surface}" />
        <Setter Property="Foreground" Value="{StaticResource Mono.Foreground}" />
        <Setter Property="BorderBrush" Value="{StaticResource Mono.Border}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="RowBackground" Value="{StaticResource Mono.Surface}" />
        <Setter Property="AlternatingRowBackground" Value="{StaticResource Mono.Panel}" />
        <Setter Property="HorizontalGridLinesBrush" Value="{StaticResource Mono.Border}" />
        <Setter Property="VerticalGridLinesBrush" Value="{StaticResource Mono.Border}" />
        <Setter Property="RowHeaderWidth" Value="0" />
    </Style>

    <Style TargetType="DataGridColumnHeader">
        <Setter Property="Background" Value="{StaticResource Mono.SurfaceRaised}" />
        <Setter Property="Foreground" Value="{StaticResource Mono.Foreground}" />
        <Setter Property="BorderBrush" Value="{StaticResource Mono.Border}" />
        <Setter Property="BorderThickness" Value="0,0,1,1" />
        <Setter Property="Padding" Value="6,4" />
        <Setter Property="FontWeight" Value="SemiBold" />
    </Style>

    <Style TargetType="DataGridCell">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Foreground" Value="{StaticResource Mono.Foreground}" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Padding" Value="4,2" />
        <Style.Triggers>
            <Trigger Property="IsSelected" Value="True">
                <Setter Property="Background" Value="{StaticResource Mono.SelectionHighlight}" />
                <Setter Property="Foreground" Value="{StaticResource Mono.Foreground}" />
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style TargetType="DataGridRowHeader">
        <Setter Property="Background" Value="{StaticResource Mono.Panel}" />
    </Style>

</ResourceDictionary>
